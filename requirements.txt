numpy==2.2.6
opencv-python==4.12.0.88
torch==2.9.1
torchvision==0.24.1
ultralytics==8.3.233
lap==0.5.12

# For torch and torchvision, ensure to install with the appropriate CUDA version if needed.
# Otherwise, the above versions will install the CPU-only versions.
# You can find the correct installation command for torch and torchvision at https://pytorch.org/get-started/locally/